# python-calculator

This code is a simple calculator made in Python. To select the operation you want to perform, enter the operation number, then select the numbers you want to work with and see the result (Currently only division, multiplication,addition,substraction and There is base acquisition.)